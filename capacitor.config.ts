import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.pilotagesoutien',
  appName: 'Pilotage Soutien',
  webDir: 'dist'
};

export default config;
